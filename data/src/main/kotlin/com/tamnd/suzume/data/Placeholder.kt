package com.tamnd.suzume.data

/**
 * Placeholder so the `:data` module compiles at M0.
 *
 * The repositories, Room database, DataStore preferences, Paging sources, and the
 * subscription poller land in later milestones (M3 onward, offline layer in M8).
 */
internal object Placeholder
